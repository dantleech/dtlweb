{% for post in posts %}
    <div class="cell panel">
        <div class="header">
            <a href="{{ path(post) }}">{{ post.title }}</a>
        </div>
        <div class="body">
            <div class="cell">
                {{ post.content|truncate(50) }}
            </div>
        </div>
    </div>
{% endfor %}
