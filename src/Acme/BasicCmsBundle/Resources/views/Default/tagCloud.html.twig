<ul class="nav cloud center">
    {% for tagName, data in tags %}
        <li style="margin: -2px; font-size: {{ data.weight * 1.2 + 0.8 }}em"><a href="{{ path('tag_referrer_index', {'tag': tagName}) }}">{{ tagName }}</a></li>
    {% endfor %}
</ul>
